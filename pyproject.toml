[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "enhanced-idealization"
version = "0.1.0"
description = "A Python MVP for generating and evaluating idealized future-state options."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "GitHub Copilot Task Agent" }
]

[tool.setuptools]
packages = ["enhanced_idealization"]
